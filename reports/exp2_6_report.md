# Отчет по эксперименту 2.6
**Оптимизационная точность против качества предсказания**

## 1. Постановка задачи
Проверить, как уменьшение оптимизационной ошибки (значение функции на train и норма градиента) связано с качеством модели на test.

## 2. Функции, данные, параметры, железо
- Train/test split: `80/20`.
- Метод оптимизации: `L-BFGS` (`L=10`, line search по сильным условиям Вульфа).
- Оракулы: `LogCoshL2Oracle` (регрессия), `ExponentialLossL2Oracle` (классификация).
- Датасеты: LIBSVM `abalone_scale`, `a9a`.
- Регуляризация: `lambda = 1/m`.
- Начальная точка: `x0 = 0`.
- Метрики:
  - регрессия: MSE (при необходимости MAE),
  - классификация: Accuracy.
- Железо: AMD Ryzen 5 5600H, RAM 16Gb.

## 3. Результаты
- Рисунок 2.6-1 (регрессия): ![](../fig/exp2_6_opt_vs_quality_regression.png)
- Рисунок 2.6-2 (классификация): ![](../fig/exp2_6_opt_vs_quality_classification.png)
- Формат графиков:
  - левая ось Y (логарифмическая): `f_train(x_k)` и `||∇f(x_k)||`,
  - правая ось: тестовая метрика.
  - ось X: номер итерации.

## 4. Выводы
- После определенного уровня оптимизационной точности улучшение test-метрики существенно замедляется.
- По наблюдаемым кривым порог «достаточной точности» достигается примерно при:
  - `eps ~ 10^-2` для регрессии,
  - `eps ~ 10^-1` для классификации.
- Дальнейшее уменьшение `f_train` и `||∇f||` в основном улучшает train-оптимизацию, но почти не влияет на обобщающую способность.
- Результат согласуется с теорией оптимизации в ML: после достижения рабочей области минимума переоптимизация train-цели не гарантирует прирост качества на test.
